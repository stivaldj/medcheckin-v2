# ACHADOS — coisas vistas fora da etapa corrente

> Regra: achou algo fora da etapa? Registra aqui e **não conserta de passagem**. Cada linha: data · etapa em que foi visto · o quê · onde · o que fazer depois.

| Data       | Visto em | Achado                                                                                                                                                                                                  | Onde                                      | Ação futura                                                                                                        |
| ---------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| 2026-08-16 | pré-E0   | A pasta do v2 já contém um `codedb.snapshot` (131 KB) gerado antes de qualquer código. Não é do projeto.                                                                                                | `./codedb.snapshot`                       | Adicionar `codedb.snapshot` ao `.gitignore` em E0; não commitar.                                                   |
| 2026-08-16 | pré-E0   | O v1 tem 24 vulnerabilidades npm (2 críticas) — o v2 nasce com `npm audit` no `check`? Decisão pendente (custo: audit quebra CI por dep transitiva).                                                    | auditoria §2                              | Decidir em E0: `npm audit --audit-level=high` como job separado não bloqueante até E8; bloqueante a partir de E8.  |
| 2026-08-16 | pré-E0   | Auditoria §5.0: o SQLite do v1 contém **910 prontuários reais**. Nada do v1 pode ser copiado como "seed".                                                                                               | `../cbd-clinical-upgrade-v1/cbd.sqlite`   | Seed do v2 é 100% sintético (E1). Nunca abrir `cbd.sqlite` a partir deste repo.                                    |
| 2026-08-16 | E0       | `npm audit`: 3 high em deps transitivas do Next (`postcss <=8.5.22`, `sharp <0.35.0`). Não bloqueiam E0.                                                                                                | `npm audit`                               | Reavaliar ao subir Next em E4; job `npm audit --audit-level=high` não bloqueante no CI (ver linha acima).          |
| 2026-08-16 | E0       | Porta 5432 do host já ocupada por outro projeto (`ibl-ai-os-db-1`); dev do v2 usa `POSTGRES_PORT=5434`.                                                                                                 | `docker-compose.yml`, `.env.example`      | Nada; parametrizado. Documentar no README se alguém tropeçar.                                                      |
| 2026-08-16 | E0       | Tailwind/shadcn/Recharts/Playwright ainda não instalados — E0 é scaffold mínimo; entram quando a primeira tela existir (E4/E5).                                                                         | `apps/web/package.json`                   | Instalar em E4 (shadcn+Tailwind), E5 (Playwright), E6 (Recharts).                                                  |
| 2026-08-16 | E2       | Testes e dev compartilham o mesmo `DATABASE_URL`; `freshDb()` faz rollback total e o último teste deixa o banco no estado dele. Funciona, mas confunde o `npm run seed`.                                | `packages/core/test/helpers/db.js`        | Em E3, `DATABASE_URL_TEST` (banco `medcheckin_test` no mesmo compose) e CI com dois bancos.                        |
| 2026-08-16 | E2       | `runCycle` guarda "última avaliação de alertas" em memória; reinício do processo reavalia imediatamente (inofensivo, mas não é durável).                                                                | `packages/core/src/scheduler/cycle.js`    | Em E6/E8, tabela `system_state(key, value, at)` para carimbos do scheduler (também para o `/health` do scheduler). |
| 2026-08-16 | E2       | Hook de segurança do Claude Code confunde `RegExp.prototype.exec` com `child_process.exec` e bloqueia o Write.                                                                                          | `next-run.js` (contornado com `.match`)   | Nada no repo; só saber.                                                                                            |
| 2026-08-16 | E3       | Sem rate limit por IP nas rotas de auth (só throttle por e-mail no link mágico; `/api/p/accept` sem limite → brute force do `invite_token` de 43 chars é impraticável, mas o log não alerta).           | `apps/web/app/api/auth/*`, `api/p/accept` | E8: rate limit no Caddy (ou middleware) + alerta operacional em `logger.warn` repetido.                            |
| 2026-08-16 | E3       | Sem proteção CSRF explícita nas rotas POST; cookie `SameSite=Lax` cobre navegação cross-site, mas `fetch` de origem estranha com cookie não passa por Lax só em GET.                                    | `apps/web/lib/auth.ts`                    | E4 (primeira tela com mutação): checar `Origin`/`Sec-Fetch-Site` nas rotas mutáveis.                               |
| 2026-08-16 | E3       | Páginas `/login`, `/hoje`, `/auth/invalido` são placeholders sem shadcn/Tailwind (E4 traz o design).                                                                                                    | `apps/web/app/*`                          | E4.                                                                                                                |
| 2026-08-16 | E3       | Item de E2 fechado: `DATABASE_URL_TEST` + banco `medcheckin_test` (compose `createdb`; CI cria no job).                                                                                                 | —                                         | —                                                                                                                  |
| 2026-08-16 | E4       | shadcn init escolheu o estilo `base-nova` (Base UI): `asChild` não existe, usa `render={<X/>}`. Componentes `select` nativos foram usados em vez do `Select` do shadcn para simplicidade/testabilidade. | `apps/web/components/ui/*`, cards         | Padronizar quando houver design pass (E9).                                                                         |
| 2026-08-16 | E4       | Página do paciente lê o core direto (server component) e as mutações vão pela API — duas fontes de código para o mesmo dado (mesma função do core, então uma fonte de verdade).                         | `app/(medica)/**`                         | OK; só não duplicar regra de negócio no web.                                                                       |
| 2026-08-16 | E4       | Ajustar dose com "abrir titulação" usa o primeiro conjunto de perguntas da clínica quando não escolhido.                                                                                                | `core/medications/adjustDose`             | E6/E9: escolha explícita quando houver >1 conjunto (UI já mostra select se >1).                                    |
| 2026-08-16 | E4       | Item de E3 fechado: `assertSameOrigin` em toda rota mutável (teste 403). Item de E3 fechado: placeholders substituídos por shadcn/Tailwind (`/login` e `/auth/invalido` continuam simples).             | —                                         | —                                                                                                                  |
| 2026-08-16 | E4       | Tela `Configurações` (perfil, respondentes convidados, sistema) não entrou em E4 (spec do prompt lista em "Telas", mas E4 = Pacientes/Paciente/Perguntas).                                              | —                                         | E7 (junto com LGPD/RUNBOOK) ou E6.                                                                                 |
