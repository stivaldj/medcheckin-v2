import { SetupWizard } from '@/components/pwa/setup/SetupWizard';

/** E9.3: "Preciso de ajuda" — refaz os passos que faltam (avisos e teste) neste celular. */
export default function AjudaPage() {
  return <SetupWizard mode="help" />;
}
